package com.fpt.workflow.resolver.expression;

/** Complete allowlist for the P0 expression language. */
public enum ExpressionOperator {
  EQ,
  NE,
  GT,
  GTE,
  LT,
  LTE,
  IN,
  CONTAINS,
  IS_NULL,
  AND,
  OR,
  NOT
}
